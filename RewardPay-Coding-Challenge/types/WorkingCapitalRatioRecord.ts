// ORM for the working capital ratio entries from raw data
export type WorkingCapitalRatioRecord = {
    account_category: string,
    total_value: number,
    value_type: string,
    account_type: string
}