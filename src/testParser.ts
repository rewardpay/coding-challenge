import { parseData } from '../utils/utils';  // Imported the parseData function

const data = parseData('./data.json');  // Used the function to parse data from data.json
//console.log(data);  // I logged thhe parse data if it works
